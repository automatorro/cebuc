# Required — Anthropic API key, used for the commercial-opportunity interpretation step.
ANTHROPIC_API_KEY=

# Optional — Google PageSpeed Insights API key. If absent, the Mobile/speed
# category is skipped gracefully and the rest of the audit still runs.
PAGESPEED_API_KEY=
